name: Clone

on: push

jobs:
  clone:
    name: Clone Repo
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-go@v3
        with:
          go-version: 1.20
          cache: false
          check-latest: false